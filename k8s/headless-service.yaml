apiVersion: v1
kind: Service
metadata:
  name: concord
  namespace: concord
  labels:
    app: concord
spec:
  clusterIP: None
  selector:
    app: concord
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
